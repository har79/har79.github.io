<h1><%= page.title %></h1>
<div>
  <ul>
    <% site.categories.findOne({name: page.category}).posts.sort('date',
    -1).forEach(post => { if (!post.hidden) { %>
    <li><a href="<%= post.path %>"><%= post.title %></a></li>
    <% }}) %>
  </ul>
</div>
