<div class="content photos">
  <h1><%= page.title %></h1>
  <%- page.content %>
  <div class="photo-gallery">
    <% page.photos.forEach(photo => { %>
    <a class="photo-link" href="<%= photo %>.jpeg">
      <img src="<%= photo %>-thumb.jpeg" />
    </a>
    <% }) %>
  </div>
</div>
